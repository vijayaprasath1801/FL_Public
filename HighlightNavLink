const isSelected = (itemRoute, currentPath) => {
  return itemRoute === currentPath;
};

  const isParentSelected =
      isSelected(item.route, window.location.pathname) ||
      item.megaMenu?.some((group) =>
        group.items.some((subItem) => isSelected(subItem.route, window.location.pathname))
      );
